rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default: Allow authenticated users to read/write all documents
    // This is TEMPORARY for development - we will add proper rules step by step
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}